<?xml version="1.0" ?>
<ruleset
    name="Custom Ruleset"
    xmlns="http://pmd.sf.net/ruleset/1.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sf.net/ruleset/1.0.0 http://pmd.sf.net/ruleset_xml_schema.xsd"
>
    <!--
    To customize PHPMD, import each ruleset, excluding rules that are not needed or need to be
    customized separately.
    @see https://phpmd.org/documentation/creating-a-ruleset.html
    -->

    <rule ref="rulesets/cleancode.xml">
        <!-- Boolean arguments are no worse than other arguments. -->
        <exclude name="BooleanArgumentFlag" />
        <!-- Avoid else expresssions does not help -->
        <exclude name="ElseExpression" />
        <!-- This does not help since = is not ===. -->
        <exclude name="IfStatementAssignment" />
        <!-- Customized below -->
        <exclude name="MissingImport" />
        <!-- Calling static functions is necessary sometimes. -->
        <exclude name="StaticAccess" />
    </rule>
    <rule ref="rulesets/codesize.xml">
        <!-- Redundant and not a good measurement. -->
        <exclude name="NPathComplexity" />
        <!-- Customized below -->
        <exclude name="TooManyMethods" />
        <!-- Customized below -->
        <exclude name="TooManyPublicMethods" />
    </rule>
    <rule ref="rulesets/controversial.xml" />
    <rule ref="rulesets/design.xml" />
    <rule ref="rulesets/naming.xml" />
    <rule ref="rulesets/unusedcode.xml" />

    <!-- Mark globals like \PDO rather than importing them -->
    <rule ref="rulesets/cleancode.xml/MissingImport">
        <properties>
            <property name="ignore-global" value="true" />
        </properties>
    </rule>

    <!-- Ignore getters, setters, and unit tests -->
    <rule ref="rulesets/codesize.xml/TooManyMethods">
        <properties>
            <property name="ignorepattern" value="/^(get|set|test)/" />
        </properties>
    </rule>

    <!-- Ignore getters, setters, and unit tests -->
    <rule ref="rulesets/codesize.xml/TooManyPublicMethods">
        <properties>
            <property name="ignorepattern" value="/^(get|set|test)/" />
        </properties>
    </rule>
</ruleset>
